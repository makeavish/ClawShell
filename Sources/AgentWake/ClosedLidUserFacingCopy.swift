enum ClosedLidUserFacingCopy {
    static let safetyNotice = "AgentWake will turn off Lid-Closed Awake automatically if battery is at or below 15% or macOS reports critical thermal pressure. Direct sensor temperature isn't wired yet, so plug into AC for long overnight runs."
}
